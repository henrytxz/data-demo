dev: # this needs to match the profile: in your dbt_project.yml file
  target: dev
  outputs:
    dev:
      type: bigquery
      method: service-account
      keyfile: /Users/henry/.dbt/dbt-user.json # Replace this with the full path to your keyfile
      project: henrytxz  # Replace this with your project id
      dataset: data_demo # Replace this with dbt_your_name, e.g. dbt_bob
      threads: 1
      timeout_seconds: 300
      location: US
      priority: interactive
